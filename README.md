# New-project
This is an item sorter

if you want to sort any directory, download the itemsorter.py file
replace the PATH with your own path.
replace the name of folders in the array to use custom folders.
and then run the command.
